# Local development: no GitHub round-trip, everyone is the "local" user.
# This file is committed on purpose and must only ever hold these harmless
# placeholders — real values belong in `wrangler secret put`, never here.
REQUIRE_AUTH=false
GITHUB_CLIENT_ID=unused-locally
GITHUB_CLIENT_SECRET=unused-locally
SESSION_SECRET=local-dev-secret
